
<% form_for @event , :url => events_path do |f|%>

	<%= f.label :name, "Name" %>
	<%= f.text_field :name %>

	<%= f.label :description, "Description" %>
	<%= f.text_area :description %>

	<%= f.label :isbn, "Isbn" %>
	<%= f.text_field :isbn %>

	<%= f.submit "Create"%>

<% end %>